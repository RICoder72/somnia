# Somnia Development Session Notes

## Session: 2026-02-03 - Dream Integration + MCP Server

### What We Did

#### Part 1: Dream Operation Integration
- Wired `extract_json_from_output()` and `apply_dream_operations()` into
  `run_consolidation()` — dreams now actually modify the graph
- Added `log_diagnostics()` for capturing graph health and token usage
- Tested a real dream cycle: 4 inbox items → 3 new nodes, 2 new edges, 0 errors

#### Part 2: MCP Design Discussion
Key decisions documented in `MCP_DESIGN.md`:
- **Write without checking**: Don't pre-validate against graph, let dreams handle
  deduplication/reinforcement. Frequency is a signal.
- **MCP as abstraction layer**: Tools hide backend details (FTS5 now, embeddings
  later) — search implementation can improve without changing the client contract.
- **Topic-derived reads**: No upfront context dump. Recall as topics develop.
- **Three initial tools**: `somnia_remember`, `somnia_recall`, `somnia_status`

#### Part 3: MCP Server Build
- Built FastMCP server (`mcp/somnia_mcp.py`) running on port 8011
- Thin wrapper over existing Flask API (internal HTTP calls)
- Updated Dockerfile to run both Flask + MCP via entrypoint script
- Rebuilt container, all three tools tested and working via MCP protocol

### Dream Results (First Real Dream)
```
Before: 3 nodes, 2 edges, 4 inbox pending
After:  6 nodes, 4 edges, 0 inbox pending
Duration: 22 seconds
Errors: none

New nodes:
  pref_tracer_bullets (preference) - tracer bullet methodology
  concept_meta_learning (concept) - procedures as experiential memory
  fact_oauth_reliability (fact) - OAuth token reliability issues
```

### Git Log After Session
```
24fb03a Add MCP server for Somnia memory integration
5c807e9 Wire dream operations into consolidation cycle
5a4ba59 Add session notes for 2026-02-03
```

### Container State
- Somnia running with Flask API (8010) + MCP server (8011/sse)
- Both on super-claude Docker network
- MCP endpoint: `http://somnia:8011/sse`

### What's Next
1. **Connect MCP to Claude** — add Somnia MCP connector in claude.ai settings
   and/or Claude Code MCP config
2. **Write global instructions** — behavioral guidance for passive memory collection
3. **Test live** — have a real conversation with Somnia active and see how it feels
4. **Iterate on write quality** — tune what gets remembered based on real usage
