[project]
name = "fabrica"
version = "1.0.0"
description = "Fabrica admin MCP — container management, config, backups"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.0.0",
    "uvicorn>=0.30.0",
]

[tool.setuptools]
py-modules = ["server"]
